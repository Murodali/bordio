export enum ColumnType {
    NEW_TASK = 'New task',
    SCHEDULED = 'Scheduled',
    IN_PROGRESS = 'In progress',
    COMPLETED = 'Completed',
    CREATED_NEW = "Create status"
  }
  
  export enum ItemType {
    TASK = 'Task',
  }